{
  "name": "verishare-evm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rimraf cache artifacts",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network $NETWORK",
    "verify": "hardhat verify --network $NETWORK $ADDRESS"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.13",
    "rimraf": "^6.0.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
